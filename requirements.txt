Flask>=3.0
python-dotenv>=1.0
Werkzeug>=3.0
# zoneinfo has no bundled tz database on slim Linux images (Raspberry Pi OS Lite
# included). Per-user timezones depend on this being present.
tzdata>=2024.1
# Receipt photos (Phase 2): resize, thumbnail, and strip the EXIF a phone camera
# writes — which includes the GPS coordinates of wherever the photo was taken.
# Prebuilt wheels exist for Raspberry Pi OS, so this needs no compiler.
Pillow>=10.0
# Serving it for real. Neither is imported by the app, and neither is needed to
# run the dev server, so both stay commented and are installed by hand on the
# machine that needs them — see DEPLOY.md.
#
#   Pi:      pip install gunicorn      (forks; the config is deploy/gunicorn.conf.py)
#   Windows: pip install waitress      (gunicorn does not run there — it forks)
# gunicorn>=21.2
# waitress>=3.0
