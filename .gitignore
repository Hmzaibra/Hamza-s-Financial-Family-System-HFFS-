.env
app.db
app.db-wal
app.db-shm
uploads/
backups/

__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.DS_Store

# Design-review screenshots. Regenerate with shots.py, shots_phase23.py or
# shots_phase4.py; they are output, and a binary diff per tweak is not history
# worth keeping.
shots/
shots2/
shots-phase23/
shots-phase4/

# Scratch used to move files onto this machine. Safe to delete.
_to_delete/

# Debris the desktop bridge's FUSE mount leaves when a file is replaced while
# something still has it open. Not ours, never wanted in a commit.
.fuse_hidden*
